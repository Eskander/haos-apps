configuration:
  PLAYWRIGHT_DRIVER_URL:
    name: Sock Puppet Browser
    description: Use a real Chrome browser (playwright protocol)
  SCREENSHOT_MAX_HEIGHT:
    name: Maximum height of screenshots
    description: Default is 16000 px, screenshots will be clipped to this if exceeded.
network:
  5000/tcp: Web interface
